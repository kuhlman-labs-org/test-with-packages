/**
 * Test Package
 * A simple test package for GitHub Packages demonstration
 */

function greet(name) {
  return 'Hello, ' + name + '!';
}

function add(a, b) {
  return a + b;
}

module.exports = {
  greet,
  add
};
